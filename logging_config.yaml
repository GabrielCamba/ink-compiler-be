appenders:
  my_stdout:
    kind: console
    encoder:
      pattern: "{h({d(%Y-%m-%d %H:%M:%S)(utc)} - {l}: {m}{n})}"
  my_file_logger:
    kind: rolling_file
    path: "log/compiler-be.log"
    encoder:
      pattern: "{d(%Y-%m-%d %H:%M:%S)(utc)} - {h({l})}: {m}{n}"
    policy:
      trigger:
        kind: size
        limit: 500kb
      roller:
        kind: fixed_window
        base: 1
        count: 10
        pattern: "log/my{}.log"
root:
  level: info 
  appenders:
    - my_stdout
    - my_file_logger
